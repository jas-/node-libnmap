os:
  - linux
  - osx

addons:
  apt:
    packages:
      - nmap

language: node_js
node_js:
  - "6"
  - "8"
  - "10"
  - "12"

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update && brew install nmap; fi
